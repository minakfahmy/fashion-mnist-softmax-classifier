# fashion-mnist-softmax-classifier
A NumPy-based implementation of a Softmax Regression classifier trained on Fashion-MNIST with cross-entropy loss, L2 regularization, and hyperparameter sweep tracking using Weights &amp; Biases (W&amp;B).
